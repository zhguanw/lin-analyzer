MESSAGE(STATUS "LLVM_TOOLS_BINARY_DIR: ${LLVM_TOOLS_BINARY_DIR}")

FIND_PATH(LLVM_BUILD_BIN_DIR lin-analyzer)
#SET(LLVM_BUILD_BIN_DIR ${LLVM_BINARY_BIN}/"bin")
MESSAGE(STATUS "LLVM_BUILD_BIN_DIR Found: ${LLVM_BUILD_BIN_DIR}")

IF (LLVM_BUILD_BIN_DIR)
	#MESSAGE(STATUS "LLVM_TOOLS_BINARY_DIR")
	SET(LLVM_BIN_DIR_FOUND TRUE)
	SET(LIN-ANALYZER ${LLVM_BUILD_BIN_DIR}/lin-analyzer)
	MESSAGE(STATUS "LLVM_BUILD_BIN_DIR: ${LLVM_BUILD_BIN_DIR}")
ENDIF (LLVM_BUILD_BIN_DIR)

IF (LLVM_BIN_DIR_FOUND)
	MESSAGE(STATUS "LLVM_BUILD_BIN_DIR found!")
ELSE (LLVM_BIN_DIR_FOUND)
        MESSAGE(FATAL_ERROR "ERROR: LLVM_BUILD_BIN_DIR NOT found!")
ENDIF (LLVM_BIN_DIR_FOUND)

IF (LLVM_BIN_DIR_FOUND AND CLANG)
	MESSAGE(STATUS "Found clang!")
ELSE (LLVM_BIN_DIR_FOUND AND CLANG)
	SET(CLANG ${LLVM_BUILD_BIN_DIR}/clang)
ENDIF (LLVM_BIN_DIR_FOUND AND CLANG)

IF (LLVM_BIN_DIR_FOUND AND OPT)
	MESSAGE(STATUS "Found opt!")
ELSE (LLVM_BIN_DIR_FOUND AND OPT)
	SET(OPT ${LLVM_BUILD_BIN_DIR}/opt)
ENDIF (LLVM_BIN_DIR_FOUND AND OPT)

IF (LLVM_BIN_DIR_FOUND AND LLC)
	MESSAGE(STATUS "Found llc!")
ELSE (LLVM_BIN_DIR_FOUND AND LLC)
	SET(LLC ${LLVM_BUILD_BIN_DIR}/llc)
ENDIF (LLVM_BIN_DIR_FOUND AND LLC)

IF (LLVM_BIN_DIR_FOUND AND LLI)
	MESSAGE(STATUS "Found lli!")
ELSE (LLVM_BIN_DIR_FOUND AND LLI)
	SET(LLI ${LLVM_BUILD_BIN_DIR}/lli)
ENDIF (LLVM_BIN_DIR_FOUND AND LLI)

IF (LLVM_BIN_DIR_FOUND AND LLVM_LINK)
	MESSAGE(STATUS "Found llvm-link")
ELSE (LLVM_BIN_DIR_FOUND AND LLVM_LINK)
	SET(LLVM_LINK ${LLVM_BUILD_BIN_DIR}/llvm-link)
ENDIF (LLVM_BIN_DIR_FOUND AND LLVM_LINK)

MESSAGE(STATUS "Found all necessary binaries!")
